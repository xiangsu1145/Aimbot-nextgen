<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Light-only: the whole app is designed around the light MD3 palette
         (hardcoded in ui/theme/Color.kt). DayNight would render Material
         widgets (buttons/chips/tabs) with the dark palette and clash with it. -->
    <style name="Theme.AimbotNextgen" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:fontFamily">sans</item>
        <item name="colorPrimary">#6750A4</item>
        <item name="colorOnPrimary">#FFFFFF</item>
        <item name="colorPrimaryContainer">#EADDFF</item>
        <item name="colorOnPrimaryContainer">#21005D</item>
        <item name="colorSecondary">#625B71</item>
        <item name="colorOnSecondary">#FFFFFF</item>
        <item name="colorSurface">#FFFBFE</item>
        <item name="colorOnSurface">#1C1B1F</item>
        <item name="colorSurfaceVariant">#E7E0EC</item>
        <item name="colorOnSurfaceVariant">#49454F</item>
        <item name="colorError">#B3261E</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>

    <!--
      Translucent variant for the menu's external Activities. Both file picker
      and text-input land on top of the daemon's SurfaceFlinger layer, so the
      activity must NOT fill the screen with an opaque App background — that
      would hide the menu behind it.
    -->
    <style name="Theme.AimbotNextgen.Translucent" parent="Theme.AimbotNextgen">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>
